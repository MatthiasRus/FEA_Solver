#include "STRRelease.hpp"
#include <iostream>
#include <string>


STRRelease::STRRelease(
	int Id;
	std::string Name,
	double KuxStart, KuyStart, KuzStart, KrxStart, KryStart, KrzStart, KuxEnd, KuyEnd, KuzEnd, KrxEnd, KryEnd, KrzEnd;
	) : Id(Id), Name(Name), KuxStart(KuxStart), KuyStart(KuyStart), KuzStart(KuzStart), KrxStart(KrxStart), KryStart(KryStart), KrzStart(KrzStart),
	KuxEnd(KuxEnd), KuyEnd(KuyEnd), KuzEnd(KuzEnd), KrxEnd(KrxEnd), KryEnd(KryEnd), KrzEnd(KrzEnd) {
	};

	void STRRelease::ToString() {

}